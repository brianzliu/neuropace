/** There is one learner page now (the dashboard), reached by the brand link. Studio (/session/new),
 * live (/live/*) and replay hide the header entirely — see App.tsx `showTabs`. */
export default function TopTabs() {
  return null;
}
